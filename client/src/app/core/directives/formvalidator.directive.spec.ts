import { FormvalidatorDirective } from './formvalidator.directive';

describe('FormvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
